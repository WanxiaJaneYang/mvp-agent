# Frontend Development Guidelines

> Current status of frontend guidance in this repo.

---

## Status

This repository currently has **no frontend source tree**.

Use the frontend spec directory only as a guardrail:
- do not invent React/TypeScript conventions for this repo yet
- do not claim frontend patterns exist when there is no code to support them
- when frontend code is added, replace these placeholders with repo-specific guidance based on real files

---

## Current Rule

If a task is backend-only or docs-only, ignore the frontend spec set.

If a future task introduces frontend code:
1. add the actual source tree
2. document the real conventions from that code
3. then treat these docs as active
