# Frontend Type Safety

No frontend type system is active in this repository today because there is no frontend source code.

Do not assume TypeScript, React props patterns, or client-side validation libraries until those files exist.
